---
const features = [
  {
    title: "Camera Isolation by Design",
    desc: "Keep cameras on a private LAN (TB1). OpenNVR ingests streams and exposes only secured restreams—reduce blast radius.",
    tag: "Security-first",
  },
  {
    title: "Secure Stream Gateway",
    desc: "TLS/SRTP-compatible restreaming, access control, and per-tenant boundaries. Don’t expose cameras directly.",
    tag: "Gateway",
  },
  {
    title: "AI Adapters & Ecosystem",
    desc: "Run ONNX/TensorRT/OpenVINO/HF adapters on your terms. Community adapters are open; verified adapters are enterprise-ready.",
    tag: "Extensible",
  },
  {
    title: "Auditability & Compliance",
    desc: "Immutable logs, exports with integrity, retention controls. Make security and AI decisions explainable.",
    tag: "Governance",
  },
  {
    title: "Offline-First Operations",
    desc: "Runs without cloud dependency. Perfect for air-gapped, regulated, or unreliable network environments.",
    tag: "Sovereign",
  },
  {
    title: "Fleet-Friendly Architecture",
    desc: "Operate multi-site deployments with policy rollout, drift detection, and node health checks (Enterprise).",
    tag: "Scale",
  },
];
---
<section class="feature-section">
  <div class="container">
    <h2 class="feature-title">What OpenNVR gives you</h2>
    <p class="feature-subtitle">
      Built for environments where cameras are untrusted, audits matter, and AI must be controlled—not a black box.
    </p>

    <div class="feature-grid">
      {features.map((f) => (
        <div class="card feature-card">
          <div class="badge feature-tag">
            <span class="feature-tag-dot"></span>
            {f.tag}
          </div>
          <div class="feature-card-title">{f.title}</div>
          <div class="feature-card-desc">{f.desc}</div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .feature-section {
    padding: var(--space-2xl) 0;
  }

  .feature-title {
    margin: 0 0 var(--space-sm);
    font-size: 28px;
  }

  .feature-subtitle {
    margin: 0 0 var(--space-lg);
    color: var(--muted);
    max-width: 850px;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-lg);
  }

  .feature-card {
    padding: var(--space-lg);
  }

  .feature-tag {
    margin-bottom: 10px;
  }

  .feature-tag-dot {
    display: block;
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: var(--brand2-dim);
    flex-shrink: 0;
  }

  .feature-card-title {
    font-weight: 800;
    font-size: 16px;
    margin-bottom: 6px;
  }

  .feature-card-desc {
    color: var(--muted);
    line-height: 1.55;
  }
</style>

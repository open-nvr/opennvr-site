<section class="cta-section">
  <div class="container">
    <div class="card cta-card">
      <div class="cta-content">
        <div class="cta-title">Want a pilot or a security assessment?</div>
        <div class="cta-desc">
          Start with a camera exposure & posture assessment. Then deploy OpenNVR to isolate cameras and gate streams securely.
        </div>
      </div>
      <div class="cta-actions">
        <a class="btn primary" href="/contact">Talk to us</a>
        <a class="btn" href="/pricing">See plans</a>
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: var(--space-2xl) 0 var(--space-6xl);
  }

  .cta-card {
    padding: 18px;
    display: flex;
    gap: var(--space-lg);
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
  }

  .cta-title {
    font-weight: 900;
    font-size: 18px;
    margin-bottom: var(--space-xs);
  }

  .cta-desc {
    color: var(--muted);
    max-width: 720px;
    line-height: 1.5;
  }

  .cta-actions {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
  }
</style>
